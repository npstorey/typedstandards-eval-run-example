# Evaluation log bytes: pinned by hash in corpus/manifest.json, never committed.
data/

# Dependencies: pinned exactly in package.json and package-lock.json.
node_modules/

# Signing seeds live outside the repository; this is a second guard.
*.seed

# Python
__pycache__/
*.pyc
.venv/

# OS
.DS_Store
