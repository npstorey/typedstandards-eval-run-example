{
  "name": "typedstandards-eval-run-example",
  "version": "0.0.0",
  "private": true,
  "description": "A public AI benchmark evaluation run as a Typed Standards record package, verifiable offline with one command.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "verify": "node verify.mjs"
  },
  "dependencies": {
    "@typedstandards/produce-core": "0.4.0",
    "@typedstandards/verify-core": "0.9.0"
  }
}
